Watch "video".

Commands used:
dc_shell -f syn_script.tcl: da keda hay run el script file beta3ak w el output beta3o hayektebo fel terminal. 
dc_shell -f syn_script.tcl > syn.log: da keda hay run el script file beta3ak w el output beta3o hayektebo fi file esmo syn.log.
dc_shell -f syn_script.tcl | tee syn.log: da keda hay run el script file beta3ak w el output beta3o hayektebo in both the terminal and syn.log file.
gui_start

Tab3an we should also add all our constraints to the script. The most professional script that you will probably always use is in the solution of lab 3.0.